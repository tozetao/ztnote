### echo
用于向终端输出字符串，配置-e选项才能才能输出反义字符

example：
```
echo 'this is demo';

echo -e "echo 3 lines\n\n\n";

echo 'the log file have be done' > log.txt
# 重定向输出

echo "hello world\""
# 引号是一个特殊字符，在输出时需要转义
```

### read
read命令用于从标准输入中输出一个变量，并把指赋予变量中。

example：
```

```