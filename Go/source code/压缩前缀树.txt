压缩前缀树
	特点：具有公共字符串前缀的节点拥有同一个父节点。

插入实现：
	插入字符串时，从根节点开始比较，如果具有相同的公共前缀，则节点需要分裂。
	分裂过程：将公共前缀作为当前节点，它会成为一个父节点。原节点的非公共前缀和插入字符串的非公共前缀要作为它的子节点。
	同时要注意分裂出来的子节点是否也具有公共前缀。

查询实现：
	将查询字符串从根节点开始比较，比较查询字符串是否包含节点字符串前缀，