封装与分用
	在TCP/IP网络协议栈中，发送数据的过程是封装，而解析数据的过程是分用。

ARP协议
	

IP地址到以太网地址（MAC地址）的转换


DNS（Domain Name Service）
	DNS提供域名拆线呢服务


2^4 = 16
16进制的1位 = 2进制的4位


ping应用程序是使用ICMP协议的么？


使用tcpdump分析数据时，前面20个字节是IP头部信息，后续的字节才是TCP头部信息。


seq、ack，使用tcpdump时，如果不用-S选项，从第三个TCP报文段开始，seq和ack都是INS初始值的偏移大小。
seq是自己序号初始值的偏移大小，ack是对方序号初始值的偏移大小。



疑问
1. socket的缓冲区大小与win窗口大小有什么关系?

